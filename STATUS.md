# Status

## Current State

Repository scaffolding is set up: environment, tooling, and lab/shared
structure are in place. No lab has completed the full pipeline
(Objective → ... → Open Questions) yet.

## Labs

| Lab | Status |
|---|---|
| 01 — Attention Is All You Need | Not started (README scaffolded) |
| 02 — BERT | Not started (README scaffolded) |

## Next Steps

- Fill in `labs/01_attention_is_all_you_need/README.md` paper metadata.
- Implement scaled dot-product attention from scratch in `notebook/`.
- Run `scripts/setup.ps1` (or `scripts/setup.sh`) once `uv` is installed
  to generate `uv.lock` and verify the environment.
